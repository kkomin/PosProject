package service;

public class WorkWageService {
    public void logout(int empId) {
        // 현재 시간

        // 해당 사원의 가장 최근 login_log 조회

        // 로그인 시간 가져오기

        // 근무 시간 분 단위로 계산

        // 시급 조회

        // 일급 계산 = 시급 / 60 * 근무시간(분)

        // worklogdao에 업데이트

        // 콘솔에 일급 출력
    }
}
